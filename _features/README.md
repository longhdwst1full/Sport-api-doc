# Feature specifications

Create one folder per business feature. Start from `../_templates/feature-spec.template.yml` before implementation. The feature spec owns UI behavior and acceptance criteria; OpenAPI owns transport fields and endpoints.
