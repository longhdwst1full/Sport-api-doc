# API agent skills

This directory contains API-local skill copies for agents that discover `.agents/skills`. `.agent/skills` and `.agent/rules` are the canonical API instructions; update mirrors in this repository together and never resolve them from the former workspace root.
