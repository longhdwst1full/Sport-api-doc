# Reusable project prompts

Store only stable, project-specific prompts here. A prompt must reference `AGENTS.md`, the relevant `.agent/rules` and `.agents/skills`; it must not duplicate large rules or contain environment secrets.
